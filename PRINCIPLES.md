# AssetsStudio 开发原则

- 状态：`active`
- 生效时间：2026-08-09
- 适用范围：AssetsStudio 的代码、模型、数据、文档、实验与发布流程

本文件名使用 `PRINCIPLES.md`。`principle` 是“单条原则”，本文件包含多条原则，因此采用复数 `principles`。

## 1. 文档是工作入口

开始主要功能开发前，必须依次阅读：

1. 本文件；
2. `docs/DEVELOPMENT.md`；
3. `docs/PRODUCT_TECH_BASELINE.md`；
4. `docs/ART_DIRECTION.md`；
5. `docs/MILESTONES.md` 与 `docs/ASSET_STATUS.json`；
6. 本次功能对应的 `docs/features/*.md` 和资产工作流。

聊天记录和记忆不能替代仓库文档。上下文压缩、换机器或换开发者后，应当只靠仓库恢复当前结论。

## 2. 一个功能只有一个当前结论

正式分支只保留当前实现、当前里程碑和重建它所需的最小依赖。失败实验可以留在 Git 历史、历史分支或明确的归档位置，但不能与当前方案混放，避免再次误用。

`docs/MILESTONES.md` 是资产状态的人工入口，`docs/ASSET_STATUS.json` 是机器入口；两者必须同步。

## 3. 主要功能先写开发文档

每个主要功能必须有独立的 `docs/features/<feature>.md`，至少记录：

- 需求、范围与非目标；
- 输入、输出和用户操作路径；
- 技术选型、选择原因与被放弃方案；
- 数据合同和模块边界；
- 已知风险、验收条件和人工审查要求；
- 实现状态与带日期的变更记录。

技术方案发生变化时，不能只修改代码；必须在同一次开发检查点中更新功能文档并说明变化原因。影响多个功能的决定还要新增或更新 ADR。

## 4. 可复现优先于偶然成功

正式结果必须保留模型、脚本、参数、随机种子、依赖说明和验证入口。不能只保存一张成功渲染图，也不能依赖另一台机器上的未记录文件。

权威模型文件（如 `.blend`、`.fbx`）与运行时/预览格式应明确区分，并记录转换过程。

## 5. 自动检查不能替代人工视觉审查

自动检查负责发现缺文件、路径污染、结构错误、明显穿模指标和不可复现问题；衣物、鞋、动作、表情及像素化结果是否可接受，最终由 Gallery、正面/侧面/背面 GIF 和人工结论决定。

任何自动分数都不能自行把 `provisional` 提升为 `accepted`。

## 6. 实验与正式内容隔离

实验输出进入被 Git 忽略的 `workspace/`。候选只有满足验收条件并经过人工审查后，才可进入 `milestones/` 或正式应用目录。

不得在正式目录中堆叠 `v1` 到 `vN` 的失败候选；保留当前版本和必要来源即可。

## 7. 及时删除，但必须可追溯

过时、重复或已被替代的正式分支内容应及时删除。删除时必须同步：

- 在 `docs/REMOVALS.md` 记录时间、对象、原因、替代项和恢复位置；
- 更新相关功能文档、索引、脚本和验证规则；
- 用 Git 提交保存删除动作。

未确认替代方案、许可证或恢复路径时，不做不可逆清理。

## 8. 小步保存并留下时间线

在可运行、可验证或可审查的检查点及时提交。Git 是精确代码时间线，`docs/DEVELOPMENT_LOG.md` 记录有意义的产品、架构、里程碑和清理事件。

一次提交应围绕一个可解释目的，并包含相应文档和验证结果；不要把大量无关实验堆入同一提交。

## 9. 不得静默替换目标

不得在未记录的情况下替换 Actor、动作、服装来源、生成方法或验收标准。若原方案失败，必须先写明证据、原因、候选替代方案及迁移影响。

用户的视觉反馈属于正式需求证据，应进入对应功能文档或人工审查记录，而不是只留在对话里。

## 10. 面向最终 2D 游戏输出

Studio 可以使用 3D 生成、绑定和预览，但最终价值由 2D/像素化游戏素材的辨识度、稳定性和批量生产能力衡量。真实布料动力学、复杂节点或高成本渲染只有在能明显改善最终输出时才进入正式范围。

## 11. 规则必须可执行

关键文档、模型和状态合同由 `tools/validate_studio.py` 检查。新增强制规则时，应尽可能同时增加自动验证；验证脚本无法覆盖的部分，必须定义人工检查入口。
