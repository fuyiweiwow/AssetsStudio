# AssetsStudio 原则

1. Studio 是通用素材实验室；项目名只能作为消费者标签，不能写成生成能力的硬边界。
2. Actor Core 永远是无头发、无衣服、无鞋、无配件、无视频五官的单一中性素体；不以完整角色直出替代模块化生产。
3. 每次只生产一个 Slot 部件。部件通过 ActorSlotProfile 的锚点、包络、父骨骼和审查规则进入组合流程。
4. 风格由 StyleProfile 与已批准风格种子共同约束。先用种子压力测试稳定性，只有 ReferenceLatent 不足时才训练 LoRA。
5. 候选只能“立即销毁”或经人工 Gate 加入本地资产库；失败内容不出现在 Studio、随机池或 Git 资产中。
6. 自动 QA 不能替代四方向人工审查，尤其要检查跨视图身份、发型长度/后部质量和部件结构一致性。
7. AccuRIG 是 Actor 一对一的手工步骤；Studio 负责选择回传 FBX、复制到 Actor 工作区、验证并生成预览，不假装自动标定。
8. 环境通过参数、环境变量、相邻目录和缓存搜索发现。文档不得依赖某台机器的盘符绝对路径。
9. 模型优先从 ModelScope 下载，避免无必要占用 VPN；模型与本地资产不提交 Git。
10. 当前工作树只保留生产链必需内容。被替代内容由 Git 历史承担恢复职责，不在当前目录继续堆叠。
