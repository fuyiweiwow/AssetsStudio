# BA 风格种子稳定化与本地资产链验证（2026-08-24）

> 复核更正：用户于后续审查中发现 `decf3169...` 正/侧为长发、背面却成为短发帽。此前“批准”结论撤销；其下游素体 `ed509f5a...` 同样失效，配件 `f9e85af2...` 因父资产失效而失效。三项已从 Studio 本地资产库物理删除；原始生成候选与本记录保留为可重建证据。

## 范围

本记录验证 AssetsStudio 的通用链路，当前样例使用 `consumer_tags = [bombadvanture, ba]` 的 `bombo_adventure_qstyle_no_face_v1`。该 Profile 不是 Studio 全局风格。

运行环境为 RTX 5070 Ti 16GB、ComfyUI 0.33.0、FLUX.2 Klein 4B FP8；模型由 ModelScope 下载并已通过真实推理烟雾测试。

## 风格种子压力测试

| Seed / Job | 主题 | 自动结果 | 人工结论 |
| --- | --- | --- | --- |
| `2026082402` / `decf3169...` | 深绿长发、暖棕肤色、浅色训练服的林地治疗者 | 自动 pass / 人工 fail | 正/侧长发但背面短发帽；撤销批准并标记 `invalidated` |
| `2026082403` / `52ab4373...` | 银发山地工匠 | fail | 背面发色漂成棕色，禁止入库 |
| `2026082404` / `162dff26...` | 赤褐发、青绿/沙色沙漠法师 | pass | 比例与无嘴鼻稳定，但服装回到默认蓝夹克/红围巾；保留为身份泄漏证据，不入库 |

首个样例 `2026082401` 的所有一致性 Gate 通过，仅旧中心偏移阈值出现 `0.082 > 0.08`。阈值校准为 `<=0.09`，仍会拒绝历史 `0.0928` 偏移；该样例未作为最终风格种子。

## 素体链

- 失败候选 `2fc28c5b...`：引用已入库风格种子，但要求短紫发时正面变紫、侧背仍继承深绿长发；颜色相关性 `0.3511`，自动拒绝。
- 失效素体 `ed509f5a...`：虽然自动 Gate 通过，但继承了正/侧长发、背面短发帽的不一致；复核后标记 `invalidated`。

结论：StyleProfile + 单图 ReferenceLatent 已能稳定控制 BA 的脸部、比例与渲染语法；大幅替换发型时仍有身份混合。当前不把这一限制伪装成“已完成任意身份风格化”。

## 配件链

1. 直接把完整素体作为 ReferenceLatent 时，输出变成“人物穿戴腰包”，不符合独立配件合同；候选 `c8d50000...` 已立即销毁。
2. 改为“素体只提供父资产/尺度谱系，图像参考使用无人物槽位权威图”后，`f9e04003...` 得到独立腰包，但写实皮革微纹理违反 StyleProfile，已销毁。
3. `f9e85af2...` 的独立配件图本身通过材质检查，但父素体已经失效；该库项已删除，必须对新素体重新生成并验证尺度和装配关系。

## 本地资产谱系

```text
style_seed decf3169...
└── base_actor ed509f5a...
    └── accessory f9e85af2...
```

三项曾先写入 `review_status = invalidated / blocked_by_parent` 以阻断引用，随后按用户要求从 `workspace/local_asset_library/` 物理删除。删除后库 API 返回 4 项且全部为 `approved`。原始候选仍位于 `workspace/local_generation/`，整个 `workspace/` 被 Git 忽略，不上传。

## LoRA 决策

当前不能宣称“LoRA 已训练”：本轮完成的是风格种子校准与 ReferenceLatent 链路。跨主题压力测试证明风格语法可用，也暴露了发型/服装身份泄漏。

下一 Gate 是收集多身份、同风格、无版权污染的批准图集并复测。若多参考/提示词解耦仍无法消除身份泄漏，再训练 BA 专属 Style LoRA；LoRA 只绑定该 StyleProfile/消费者标签，不能成为 Studio 全局依赖。当前 16GB 机器保留为推理与数据准备节点，训练使用满足训练配置显存要求的机器或经单独验证的低显存方案。

## 远程审查

Tailnet-only Studio：`http://desktop-dk81254.tailf01571.ts.net:8080/`

已验证页面、API v3、本地资产列表及样例 PNG 均返回 HTTP 200。当前 tailnet 未启用 HTTPS 证书，因此使用 Tailnet 内部 HTTP 8080；ComfyUI 仍只监听 `127.0.0.1:8190`，未暴露。

## 发型拓扑增强复测

用户发现旧种子的正/侧长发与背面短发帽冲突后，新增后端强制人工 Gate：发型长度、后部体量、侧发、刘海和分缝必须构成同一三维拓扑；未提交全部人工确认时，`accept` API 返回 `409`，不能只靠前端按钮约束。

六组 Style Seed 对照：

| Seed / Job | 人工拓扑结果 | 处理 |
| --- | --- | --- |
| `2026082501` / `9be2b5c6...` | 短发可兼容，但正面出现嘴线 | 拒绝 |
| `2026082502` / `c7bdb519...` | 短发兼容，但复制默认围巾/腰包 | 拒绝 |
| `2026082503` / `d70bce27...` | 短发前/侧/背后缘兼容，无嘴鼻，无额外道具 | 批准短发种子 |
| `2026082511` / `badc9709...` | 未遵守肩长发描述，且自动 Gate 失败 | 拒绝 |
| `2026082512` / `74e7accb...` | 长发侧面与背面均到上背，同一后发帘，无嘴鼻 | 批准长发种子 |
| `2026082513` / `016014b8...` | 波波头拓扑兼容，但复制默认冒险服/腰包 | 拒绝 |

二级素体复测：

- `b6d21a78...` 引用短发种子，生成深灰训练服素体；短发在侧/背均止于后颈，全部人工 Gate 通过并批准。
- `22528134...` 引用长发种子，生成深蓝训练服素体；侧发和背部发帘均到上背，全部人工 Gate 通过并批准。

重新生成腰包 `1389debc...` 时，侧/背视图混入人物头部且三栏高度 CV 为 `0.1972`，自动与人工 Gate 均失败。因此当前只有两套风格种子和两套素体恢复为 `approved`；配件仍无重新批准项。

## 短发素体 Hunyuan3D 受控 PoC

> 2026-08-24 产品边界复核：本节中的“素体”带有头发、大眼睛、训练服和鞋，实际应称为“完整角色风格锚点”。它验证了图像到 Hunyuan 再到 Blender 的技术链路，但不符合 canonical Actor Core 合同。对应两个 2D `base_actor` 库项和一个 3D 候选已从 Studio 生命周期目录移除，禁止作为身体核心、配件父资产或 Gallery 候选继续使用。

选择已批准短发素体 `b6d21a78...`，把联合图按真实顺序拆为 `front/right/back`，仅因 Hunyuan 输入合同需要而把 right 水平镜像为 left。四张图使用本机 Hunyuan 官方去背景模块生成 RGBA；没有下载新模型，也没有把联合原图直接送入 3D。

本机入口已改为自动发现 Hunyuan 源码、2MV 模型根与 checkpoint 子目录，不再默认旧机器的盘符。使用本地标准 `hunyuan3d-dit-v2-mv`、seed `2026082403`、20 步、guidance `5.0`、octree `256` 得到首个形状候选：

- 126,388 顶点、252,772 面；峰值 CUDA 分配 5,828,215,296 bytes；
- 单一连通组件、watertight、winding consistent、Euler number 2；
- Blender 正/右/背/左审查确认大头短肢、短发后脑和服装轮廓均被保留，没有再次出现发长冲突或明显肢体粘连。

该结果当时只通过了 `shape source intake` 技术 Gate，但复核后确认输入类别错误，因此不再保留为 3D Actor 候选。下一阶段从真正光头、无耳、无五官、无服装/鞋/饰品的单一 Actor Core 重新开始；头发、Face/EyeAssembly、耳朵与所有穿戴物分别走 Slot 生成和编译，再由 Recipe 脚本装配。

## Canonical Actor Core 纠偏复测

StyleProfile 新增 `actor_core_contract`，将完整角色可用的眼睛、发型和穿戴规则从 Actor Core 的提示合同中排除。已入库风格种子仍作为版本谱系和消费者标签来源，但其人物像素不再直接进入 Actor ReferenceLatent，避免把身份部件复制到身体核心。

隔离对照结果：

- `c3fb1104...`：仍出现耳朵、侧面眼睛/眉毛、袖口、裤脚和靴形分界，销毁；
- `8f03946a...`：已无服装，但仍出现耳朵及侧面鼻嘴起伏，销毁；
- `74d6a38f...`：大部分符合，但右侧仍有耳朵浮雕，销毁；
- `0ef398ca...`：正面/右侧/背面可由手掌、脚趾和脚跟方向辨认；光头、无耳、无五官、无衣鞋饰品和材质分区，已作为 `asset_role = canonical_actor_core` 写入本地 2D 资产库。

`0ef398ca...` 已完成三视图注册、right→left 镜像、官方 Hunyuan 去背景、Hunyuan3D-2MV 20 步 shape 和 Blender 四向审查。高模为 118,564 顶点、237,124 面、单一几何体/单连通组件、watertight、winding consistent、Euler 2；峰值 CUDA 分配 5,828,215,296 bytes。用户确认形体后，它已经进入 `workspace/local_3d_asset_library/base_actors/`。入库语义仍只是无纹理 shape source，不表示已绑定或可进入 Gallery。

绑定准备采用非破坏性副本，不覆盖入库高模：

- `references/actor_core/rig_landmark_profiles/chibi_featureless_v1.json` 记录按 Actor 高度归一化的躯干、四肢、足向和 `EarRoot_L/R` 临时点；第二版四向标定图已消除第一版手骨越界和腿骨过度内收，但 AccuRIG 内仍须人工确认；
- GLB 法线分裂产生的重合顶点只在绑定副本中以 `1e-6 m` 容差焊接，再减面到 61,002 顶点 / 122,000 面；结果保持单连通、watertight、manifold，三轴尺寸最大相对漂移约 `0.000007`；
- 与入库高模的 768px 四向轮廓最差 IoU 为 `0.999944`，边界框无像素漂移；
- AccuRIG 输入 FBX 往返验证保持单网格、无 Armature、61,002 顶点 / 122,000 面、米制、落地并位于 `X=0` 中心面；首版按每手 0 根手指骨绑定，因为可见指形只是低置信度 Hunyuan 表面起伏；
- 该结果是绑定候选，不冒充生产级四边面重拓扑。下一 Gate 是 AccuRIG 人工落点、绑定和肩/肘/髋/膝基础动作变形；通过后再建立 ActorProfile，第一个独立部件仍固定为 `head_hair`。

Tailnet 人工审查入口：`http://desktop-dk81254.tailf01571.ts.net:8080/`。
