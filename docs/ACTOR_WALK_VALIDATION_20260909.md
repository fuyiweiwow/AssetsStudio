# 修复后 Actor 走路与四权重验证

当前 Actor 已通过走路骨骼适配和实际四权重 GLB 的 71 帧数值检查；脑壳保持稳定。四权重候选针对本条 Walk 优化，仍待人工外观审核、更多动作和碰撞检查，未入库。

## 修复与证据

旧适配器假定 Actor 为 Relaxed-A，剥离源动作的手臂下放偏移。新 Actor 实际为 T-pose，沿用旧方法会保留平举手臂。现在按双侧上臂静态方向识别 T-pose，对其手臂应用源骨骼的世界旋转增量；Relaxed-A 保留原策略，并及时更新骨骼依赖后再测量轨迹。

- 新 Actor 双手前后轨迹相关性 **-0.984766**，原失败值为 -0.481345；门槛仍为 <= -0.5。
- 22 骨映射、71 帧、动作幅度、左右手顺序和双手反向摆动全部通过。
- 旧 Relaxed-A Actor 实际回归也通过，相关性 **-0.810950**，没有强制切换成 T-pose 算法。

首次四权重 GLB 采用导出器默认的“取最大四个权重”，局部逐帧误差最大 **0.013373H**，超过预设 **0.005H** 上限，已拒绝为交付版本。脑壳仍刚性，问题出在其他蒙皮区域的权重裁剪。

最终候选从每个顶点原有骨骼影响中选择最多四个，以完整 Walk 帧序列拟合非负、和为一的权重。只更改运行时副本的 6,097 个顶点权重，网格坐标不变；原始八权重修复版保留为权威输入。

- 四权重拟合最大逐帧偏差约 **0.001211H**（全高的 0.1211%）。
- 实际导出的 GLB 重新导入后，再对全部 71 帧检查：每顶点最多 4 权重、归一化、时长、顶点对应、头部刚性和导出误差检查通过。
- 四向预览由实际重新导入的 GLB 渲染，不用高权重 Blend 替代运行时结果。

## 换机复现

源文件与已验证头部修复见 `milestones/actor_head_repair_20260909/`，最新 Walk 候选及证据位于其中 `walk/`。只需 Python 标准库与 Blender 4.5，自带 NumPy，无需 AI 模型、GPU 推理或再次手工标定：

```powershell
python tools/model_test/actor_head_checkpoint.py walk --output workspace/walk_replay
```

输出目录必须不存在。该命令依次重放头部修复、适配 Walk、拟合四权重、重新导入 GLB 并逐帧比较，成功输出 `WALK_RUNTIME_REPLAY_PASS`。`runtime4/runtime4.glb` 是本条 Walk 的运行时候选，`runtime_review/` 是实际运行时预览及报告。

## 剩余边界

本条动作上的拟合误差降低，不代表新四权重可泛化到跑、跳、挥手或极端扭颈。推广前必须追加独立动作与原有头颈探针；不能将 Walk 拟合覆盖八权重权威文件。尚未认证全身自交、脚底滑动、根位移或完整碰撞；极端颈肩褶皱仍见 [头部修复说明](ACCURIG_HEAD_REPAIR_20260909.md)。下一次人工审核只需观看四向 Walk 预览，无需重新标定。
